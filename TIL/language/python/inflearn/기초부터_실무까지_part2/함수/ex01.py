# 함수(function)에 대한 실습
# 1. 함수는 프로그램 안에서 중복된 코드를 제거한다.
# 2. 복잡한 프로그래밍 작업을 더 간단한 작업들로 분해할 수 있다.
# 3. 함수는 한번 만들어두면 재사용이 얼마든지 가능하다
# 4. 함수를 사용하면 가독성이 증대되고, 유지관리도 쉬워진다.

from hello import *

say_hello_name("song") # 함수 호출(function call) + 매개변수
say_hello_msg("song","how are u??")

get_sum(1,5)
get_sum(4,5)
get_sum(4,20)