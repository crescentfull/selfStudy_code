/* 사용자 헤더 파일 */

#include <stdio.h> // 표준 입출력 라이브러리를 포함. printf 함수를 사용하기 위해 필요

#define ADD(x,y) ((x)+(y)) // 매크로 정의
        // 두 개의 인수 x, y를 더하는 매크로 함수 ADD를 정의, 괄호를 사용하여 계산식을 정확하게 표현

#define SUB(x,y) ((x)-(y))
        // 두 개의 인수 x, y를 빼는 매크로 함수 SUB를 정의, 괄호를 사용하여 계산식을 자세하게 표현